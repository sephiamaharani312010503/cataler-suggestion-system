/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["placehold.jp", "firebasestorage.googleapis.com"],
  },
};

export default nextConfig;
